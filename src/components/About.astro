---
---

<section id="about" class="relative h-screen py-16 px-6 overflow-hidden text-gray-900">
  <div class="max-w-4xl mx-auto h-full flex flex-col">
    <!-- Title at top -->
    <div class="mt-16">
      <h2 class="text-3xl md:text-6xl font-light">
        Hi there!
      </h2>
    </div>
    
    <!-- Content centered -->
    <div class="flex-1 flex items-center" id="about-content">
        <div class="w-full space-y-16">
          <p class="text-2xl md:text-4xl font-light leading-relaxed md:leading-relaxed text-gray-900 about-sentence" data-sentence="1">
            I'm a <span class="keyword" data-keyword="frontend">frontend</span> engineer who believes <span class="keyword" data-keyword="software">software</span> should feel as good as it looks.
          </p>
          
          <p class="text-2xl md:text-4xl font-light leading-relaxed md:leading-relaxed text-gray-900 about-sentence" data-sentence="2">
            With roots in <span class="keyword" data-keyword="design">design</span> and <span class="keyword" data-keyword="product">product</span> ownership, I blend strategy, creativity, and code to build products people love to use.
          </p>
          
          <p class="text-2xl md:text-4xl font-light leading-relaxed md:leading-relaxed text-gray-900 about-sentence" data-sentence="3">
            From planning to shipping final builds, I'm all about making the <span class="keyword" data-keyword="journey">journey</span> just as fun as the destination.
          </p>
        </div>
    </div>
  </div>
</section>

<style>
  /* Initial state - sentences hidden */
  .about-sentence {
    opacity: 0;
    transform: translateY(60px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
    pointer-events: none;
  }
  
  /* Keywords styling - typography emphasis */
  .keyword {
    font-weight: 800;
    color: #343841;
    opacity: 0;
    transform: scale(0.9);
    display: inline-block;
    transition: all 0.6s ease-out;
    letter-spacing: -0.02em;
    padding-left: 0.25em;
    padding-right: 0.25em;
  }
  
  /* Revealed states */
  .sentence-revealed {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }
  
  .keyword-revealed {
    opacity: 1;
    transform: scale(1.08);
  }
  
  .keyword-highlighted {
    font-weight: 900;
  }
  
  /* Hide non-current sentences completely */
  .about-sentence:not(.sentence-revealed) {
    opacity: 0;
    visibility: hidden;
  }
</style>

<script>
  import { initAboutAnimation } from '../scripts/about-animation.ts';

  document.addEventListener('DOMContentLoaded', () => {
    initAboutAnimation();
  });
</script>