---

---

<section
  id="about"
  class="relative min-h-screen py-16 px-6 overflow-hidden text-gray-900"
>
  <div class="max-w-4xl mx-auto min-h-full flex flex-col">
    <!-- Content centered -->
    <div class="flex-1 flex items-center justify-center" id="about-content">
      <div class="w-full space-y-8 md:space-y-16">
        <!-- Title at top -->
        <div class="my-8 md:my-16">
          <h2 class="text-3xl md:text-6xl font-light">Hi there!</h2>
        </div>
        <p
          class="text-2xl md:text-3xl font-light leading-relaxed md:leading-relaxed text-gray-900 about-sentence"
          data-sentence="1"
        >
          I'm a <span class="keyword" data-keyword="frontend">frontend</span
          >-focused fullstack engineer who believes <span
            class="keyword"
            data-keyword="software">software</span
          > should be functional and delightful.
        </p>

        <p
          class="text-2xl md:text-3xl font-light leading-relaxed md:leading-relaxed text-gray-900 about-sentence"
          data-sentence="2"
        >
          My background in <span class="keyword" data-keyword="design"
            >design</span
          > and <span class="keyword" data-keyword="product">product</span> ownership
          allows me to bring strategy, creativity, and coding skills to build products
          people genuinely enjoy.
        </p>

        <p
          class="text-2xl md:text-3xl font-light leading-relaxed md:leading-relaxed text-gray-900 about-sentence"
          data-sentence="3"
        >
          From planning to prototyping to deploying, I'm committed to making the
          collaborative <span class="keyword" data-keyword="journey"
            >journey</span
          > as fun as the destination.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  /* Initial state - sentences hidden */
  .about-sentence {
    opacity: 0;
    transform: translateY(60px);
    transition:
      opacity 0.8s ease-out,
      transform 0.8s ease-out;
    pointer-events: none;
  }

  /* Keywords styling - typography emphasis */
  .keyword {
    font-weight: 800;
    color: #343841;
    opacity: 0;
    transform: scale(0.9);
    display: inline-block;
    transition: all 0.6s ease-out;
    letter-spacing: -0.02em;
    padding-left: 0.25em;
    padding-right: 0.25em;
  }

  /* Revealed states */
  .sentence-revealed {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  .keyword-revealed {
    opacity: 1;
    transform: scale(1.08);
  }

  .keyword-highlighted {
    font-weight: 900;
  }

  /* Hide non-current sentences completely */
  .about-sentence:not(.sentence-revealed) {
    opacity: 0;
    visibility: hidden;
  }
</style>

<script>
  import { initAboutAnimation } from "../scripts/about-animation.ts";

  document.addEventListener("DOMContentLoaded", () => {
    initAboutAnimation();
  });
</script>
