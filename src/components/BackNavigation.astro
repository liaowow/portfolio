---
const baseUrl = import.meta.env.BASE_URL;
---

<section class="py-12 px-6 bg-gray-50 border-t border-gray-200">
  <div class="max-w-4xl mx-auto text-center">
    <a
      href={`${baseUrl}#work`}
      class="inline-flex items-center gap-2 px-6 py-3 bg-gray-900 text-white hover:bg-gray-700 rounded-xl font-semibold transition-all hover:-translate-y-0.5 hover:shadow-lg"
    >
      <svg
        class="w-5 h-5"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
      </svg>
      Back to All Projects
    </a>
    <p class="text-gray-600 mt-2">
      Explore more case studies and technical projects
    </p>
  </div>
</section>
